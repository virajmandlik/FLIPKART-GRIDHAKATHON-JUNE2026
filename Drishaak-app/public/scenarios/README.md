# Scenario visuals

PNG assets (`scenario-{id}.png`) are optional. When missing, the app renders
SVG junction scenes via `CctvScene` / `ScenarioFrame` with per-scenario layout,
lighting, and detection geometry from `scenarios.ts`.
