# Gridlock 2.0 — pinned-ish deps (Colab already ships most of these).
# The script auto-installs anything missing on Colab; nothing is installed locally
# unless you run it locally.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
optuna>=3.4
